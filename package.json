{
  "name": "prosemirror-html-table-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "author": "vndmea",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vndmea/prosemirror-html-table.git"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "clean": "rimraf packages/*/dist examples/*/dist coverage",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:debug": "playwright test --debug",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:codegen": "playwright codegen http://127.0.0.1:4173",
    "test:e2e:demo": "start-server-and-test \"npm run dev --workspace vue3-tiptap-table-demo -- --host 127.0.0.1 --port 4173\" http://127.0.0.1:4173 \"playwright test\"",
    "test:e2e:demo:headed": "start-server-and-test \"npm run dev --workspace vue3-tiptap-table-demo -- --host 127.0.0.1 --port 4173\" http://127.0.0.1:4173 \"playwright test --headed\"",
    "test:e2e:demo:debug": "start-server-and-test \"npm run dev --workspace vue3-tiptap-table-demo -- --host 127.0.0.1 --port 4173\" http://127.0.0.1:4173 \"playwright test --debug\"",
    "test:e2e:demo:ui": "start-server-and-test \"npm run dev --workspace vue3-tiptap-table-demo -- --host 127.0.0.1 --port 4173\" http://127.0.0.1:4173 \"playwright test --ui\"",
    "playwright:install": "playwright install",
    "playwright:install:with-deps": "playwright install --with-deps",
    "coverage": "vitest run --coverage",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "@eslint/js": "^9.0.0",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "eslint": "^9.0.0",
    "rimraf": "^6.0.0",
    "start-server-and-test": "^2.0.10",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
